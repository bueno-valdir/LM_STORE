---
/**
 * Cabecalho: logo, busca e navegacao (com menu de categorias do painel).
 * Preto com texto branco e acentos em rosa. Responsivo (menu no celular).
 */
import Logo from './Logo.astro';
import { withBase } from '../lib/url';

const linksNav = [
  { href: '/', rotulo: 'Início' },
  { href: '/catalogo', rotulo: 'Catálogo' },
  { href: '/catalogo#promocoes', rotulo: 'Promoções' },
  { href: '/entregas-e-pagamento', rotulo: 'Entregas' },
  { href: '/sobre', rotulo: 'Sobre' },
];
const acaoBusca = withBase('/catalogo');
---

<header class="sticky top-0 z-40 border-b border-white/10 bg-preto text-white">
  <!-- Linha principal: logo + busca + menu mobile. -->
  <div class="container-loja flex items-center gap-3 py-3">
    <a href={withBase('/')} class="shrink-0">
      <Logo />
    </a>

    <!-- Busca (desktop). -->
    <form action={acaoBusca} method="get" role="search" class="hidden flex-1 md:block">
      <div class="relative mx-auto max-w-xl">
        <input
          type="search"
          name="q"
          placeholder="O que você está procurando?"
          aria-label="Buscar produtos"
          class="w-full rounded-full border border-white/15 bg-white/5 px-4 py-2 pr-10 text-sm text-white placeholder:text-white/50 focus-visible:border-marca"
        />
        <button
          type="submit"
          aria-label="Buscar"
          class="absolute right-1 top-1/2 grid h-8 w-8 -translate-y-1/2 place-items-center rounded-full text-white/70 hover:text-marca-claro"
        >
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
            <circle cx="11" cy="11" r="7" />
            <path d="M21 21l-4.3-4.3" />
          </svg>
        </button>
      </div>
    </form>

    <!-- Menu no celular. -->
    <details class="relative ml-auto md:hidden">
      <summary
        class="grid h-10 w-10 cursor-pointer list-none place-items-center rounded-marca border border-white/20 text-white"
        aria-label="Abrir menu"
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path d="M4 6h16M4 12h16M4 18h16" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
        </svg>
      </summary>
      <div class="absolute right-0 mt-2 w-64 rounded-marca border border-white/10 bg-preto-claro p-3 shadow-marca">
        <form action={acaoBusca} method="get" role="search" class="mb-2">
          <input
            type="search"
            name="q"
            placeholder="Buscar..."
            aria-label="Buscar produtos"
            class="w-full rounded-full border border-white/15 bg-white/5 px-3 py-2 text-sm text-white placeholder:text-white/50 focus-visible:border-marca"
          />
        </form>
        <nav aria-label="Navegação">
          {
            linksNav.map((link) => (
              <a
                href={withBase(link.href)}
                class="block rounded-lg px-3 py-2 text-sm font-medium text-white/80 transition-colors hover:bg-white/10 hover:text-marca-claro"
              >
                {link.rotulo}
              </a>
            ))
          }
        </nav>
      </div>
    </details>
  </div>

  <!-- Linha de navegacao (desktop). -->
  <nav class="hidden border-t border-white/10 md:block" aria-label="Navegação principal">
    <div class="container-loja flex items-center gap-6 py-2 text-sm">
      {
        linksNav.map((link) => (
          <a
            href={withBase(link.href)}
            class="font-medium text-white/80 transition-colors hover:text-marca-claro"
          >
            {link.rotulo}
          </a>
        ))
      }

      <!-- Menu de categorias (preenchido pelo navegador, a partir do painel). -->
      <details class="relative">
        <summary class="flex cursor-pointer list-none items-center gap-1 font-medium text-white/80 hover:text-marca-claro">
          Categorias
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M6 9l6 6 6-6" /></svg>
        </summary>
        <div
          id="menu-categorias"
          class="absolute left-0 top-full z-50 mt-1 min-w-48 rounded-marca border border-white/10 bg-preto-claro p-2 shadow-marca"
        >
          <p class="px-3 py-2 text-xs text-white/50">Carregando...</p>
        </div>
      </details>
    </div>
  </nav>
</header>

<script>
  import { carregarProdutos, rotuloCategoria, esc } from '../lib/clienteProdutos';
  import { withBase } from '../lib/url';

  const menu = document.getElementById('menu-categorias');
  if (menu) {
    carregarProdutos()
      .then((produtos) => {
        const cats = Array.from(new Set(produtos.map((p) => p.categoria).filter(Boolean)));
        if (!cats.length) {
          menu.innerHTML = '<p class="px-3 py-2 text-xs text-white/50">Em breve</p>';
          return;
        }
        menu.innerHTML = cats
          .map(
            (c) =>
              `<a href="${withBase('/catalogo')}?cat=${encodeURIComponent(c)}" class="block rounded-lg px-3 py-2 text-sm text-white/80 transition-colors hover:bg-white/10 hover:text-marca-claro">${esc(rotuloCategoria(c))}</a>`,
          )
          .join('');
      })
      .catch(() => {
        menu.innerHTML = '<p class="px-3 py-2 text-xs text-white/50">Indisponível</p>';
      });
  }
</script>
